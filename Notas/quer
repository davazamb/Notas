var resultado = context.BaseB
    .Where(b => b.BaseA.Date < dateInsert && b.BaseA.ClCountry == "US")
    .SelectMany(b => b.BaseC, (b, c) => new { b, c })
    .OrderByDescending(x => x.b.BaseA.SomeProperty) // Cambia SomeProperty por la propiedad real
    .Take(100)
    .ToList();